name: CI
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: Test
      run: sudo wget -O /etc/systemd/system/danted.service https://raw.githubusercontent.com/raiyan088/Shell/refs/heads/main/danted.service
    
